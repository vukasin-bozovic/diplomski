import controller from './main-footer.controller';
import template from './main-footer.tpl.html';

const MainFooterComponent = {
    template,
    controller,
    controllerAs: 'mainFooterCtrl',
    transclude: true,
};

export default MainFooterComponent;
